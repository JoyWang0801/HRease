services:
  frontend:
    build: ./frontend
    ports:
      - "3010:3000"

  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - type: bind
        source: ./backend/pocketbase/pb_data
        target: /pb/pb_data

volumes:
  myapp: